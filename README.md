# Lista de Tarefas com Python

Um projeto simples usando:
- **Python** + **Flask** (back-end)
- **SQLite** (banco de dados)
- **HTML/CSS** (front-end)

## Como rodar localmente

1. Instale o Flask:
   ```cmd
   pip install flask